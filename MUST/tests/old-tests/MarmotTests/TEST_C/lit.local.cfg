# -*- Python -*- vim: set ft=python ts=4 sw=4 expandtab tw=79:

import os
from typing import List

# Tell pylint that we know config and lit_config exist somewhere.
if 'PYLINT_IMPORT' in os.environ:
    config = object()
    lit_config = object()

config.excludes = [
    "mpi2_get2.c",
    "mpi2_type_struct_optional_err_f.c",
    "omp_coll_comm.c",
    "sync_test.c",
]
