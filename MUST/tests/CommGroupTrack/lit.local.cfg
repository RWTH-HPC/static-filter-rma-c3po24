config.excludes = ["modules"]
commChecks = ["CHECK"]
if "HAVE_MPI_COMM_CREATE_FROM_GROUP" in config.available_features:
    commChecks.append("CHECK-CCFG")
if "HAVE_MPI_COMM_IDUP_WITH_INFO" in config.available_features:
    commChecks.append("CHECK-CIWI")

config.substitutions.append(("%must-comm-checks", "--check-prefixes " + ",".join(commChecks)))

