#!/bin/bash -x
#SBATCH --job-name="miniVite_base_base_16"
#SBATCH --mail-user=
#SBATCH --mail-type=NONE
#SBATCH --nodes=1
#SBATCH --ntasks=32
#SBATCH --cpus-per-task=2
#SBATCH --time=00:30:00
#SBATCH --output=job.out
#SBATCH --error=job.err
#SBATCH --partition=c23test
#SBATCH --gres=NONE
#SBATCH --account=rwth1269
#SBATCH --exclusive
#SBATCH --exclude="i23m[0001-0030]"
#SBATCH --ntasks-per-node=48
${perftool_job_config}

export OMP_NUM_THREADS="1"
export time_output_file="/rwthfs/rz/cluster/home/ss540294/research/RMA_Codes/jube/benchmarks/miniVite/miniVite.benchmarks/000025/000002_compile_base/work/time.out"
export TSAN_OPTIONS="$TSAN_OPTIONS ignore_noninstrumented_modules=1 exitcode=0 log_path=stdout"


echo "nodelist=$SLURM_JOB_NODELIST"     
          SOURCE_DIR=compile/copy_source;
          LAYOUT_DIR=$PWD;
          COMPILE_DIR=compile;
          module use /home/rwth1269/modules/; module purge; for modulename in  intel/2023a  Classic-Flang/16.0.4-c23 CMake/3.26.3 CMake/3.26.3; do module load $modulename; done;
          for item in  MPICH_CC=clang MPICH_CXX=clang++ ${openmp_export} PATH=//rwthfs/rz/cluster/home/ss540294/research/RMA_Codes/jube/benchmarks/miniVite/../../dep/MUST/47a317bbbed9c779be827373644ebbed/install/bin:$PATH; do export "$item"; done;

JUBE_ERR_CODE=$?
if [ $JUBE_ERR_CODE -ne 0 ]; then
    touch error
    exit $JUBE_ERR_CODE
fi

 srun  compile/miniVite -f /hpcwork/rwth1269/nlpkkt240.bin

JUBE_ERR_CODE=$?
if [ $JUBE_ERR_CODE -ne 0 ]; then
    touch error
    exit $JUBE_ERR_CODE
fi

printf "EXECUTION VERIFICATION CHECK: ";if grep -q "\[MUST-REPORT\] Error.*race" job.out || grep -q "^srun: error:" job.err; then echo "FAILURE"; else echo "SUCCESS"; fi

JUBE_ERR_CODE=$?
if [ $JUBE_ERR_CODE -ne 0 ]; then
    touch error
    exit $JUBE_ERR_CODE
fi

touch ready
